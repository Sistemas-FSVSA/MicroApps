<style>
    .card {
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .card-body h1 {
        font-size: 4rem;
        margin-bottom: 20px;
    }

    .no-pedidos-wrapper {
        position: relative;
        min-height: calc(100vh - 180px);
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        width: 100%;
    }

    .no-pedidos h2 {
        font-size: 2rem;
        color: #6c757d;
        font-weight: 500;
    }

    .row.g-3>div {
        display: flex;
    }

    .estado-btn {
        font-size: 1rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .card {
        border-radius: 16px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card:hover {
        transform: translateY(-4px);
        box-shadow: 0 6px 14px rgba(0, 0, 0, 0.12);
    }

    .estado-badge {
        padding: 4px 10px;
        border-radius: 6px;
        font-weight: bold;
        font-size: 0.9rem;
        color: white;
    }

    .badge-recepcion {
        background-color: #17a2b8;
    }

    .badge-cerrado {
        background-color: #dc3545;
    }

    /* Estilos para las cards de pedidos */
    .card-pedido {
        background: white;
        border-radius: 12px;
        padding: 20px;
        margin: 15px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        text-align: center;
        transition: transform 0.2s ease;
    }

    .card-pedido:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(0,0,0,0.15);
    }

    .card-pedido .icono {
        margin-bottom: 15px;
        color: #007bff;
    }

    .card-pedido .badge {
        background-color: #007bff;
        color: white;
        padding: 4px 8px;
        border-radius: 4px;
        font-size: 0.85rem;
    }

    .card-pedido .estado {
        background-color: #28a745;
    }

    .btn-detalle {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 8px 16px;
        border-radius: 4px;
        cursor: pointer;
        margin-top: 10px;
    }

    .btn-detalle:hover {
        background-color: #0056b3;
    }

    #contenedorVariosPedidos {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
    }
</style>

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2 align-items-center d-flex justify-content-between">
            <div class="col-sm-4 d-flex align-items-center">
                <button class="btn btn-outline-secondary mr-2" onclick="irAtras()">
                    <i class="fas fa-angle-double-left"></i> Regresar
                </button>
                <h1 class="m-0" id="title">Varios Pedidos</h1>
            </div>
            <div class="col-sm-6 text-right">
                <h3 id="fechaActual" class="m-0"></h3>
                <span class="text-muted">Administración</span>
            </div>
        </div>
    </div>
    <hr>
</div>

<div class="content">
    <div class="container-fluid">
        <!-- 🔹 Contenedor correcto que busca el JavaScript -->
        <div id="contenedorVariosPedidos"></div>
    </div>
</div>

<script src="/controllers/compras/variospedidos.js"></script>

<script>


    
</script>